.card-wrapper {
  position: relative; /* Establish stacking context */
  width: 100%;
  margin-bottom: 20px; /* Add space between stacked cards */
    perspective: 1000px;
}

/* FRONT & BACK SHARED */
.card-front, .card-back {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  width: 100%;
  height: 100%;
  backface-visibility: hidden;
  transition: transform 0.6s ease;
  border-radius: 12px;
  padding: 15px;
  background-size: cover;
  background-position: center;
  box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
}

/* FRONT OF THE CARD */
.card-front {
  position: relative;
  z-index: 2; /* Above the avatar */
  background-image: url('Assets/card-front.png');
  background-repeat: no-repeat;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  overflow: hidden;
}

.card-front .survivor-header {
  position: absolute;
  top: 75px;
  left: 50%;
  transform: translateX(-50%);
  width: 80%;
  font-size: 32px;
  font-weight: bold;
  font-family: 'Survivant', fantasy;
  color: #3a1f00;
  text-align: center;
  text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.6);
  z-index: 3;
  pointer-events: none;
}

.card-wrapper .avatar-frame {
  position: absolute;
  top: 96px; /* Adjust if needed */
  left: 50%;
  transform: translateX(-50%);
  width: 220px;
  height: 220px;
  overflow: hidden;
  border-radius: 12px;
  z-index: 1; /* Lower than .card-front (2), but not negative */
  display: flex;
  justify-content: center;
  align-items: center;
}

.card-wrapper .avatar-frame img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: top center;
  display: block;
}

/* BACK OF THE CARD */
.card-back {
  transform: rotateY(180deg);
  background-image: url('Assets/card-back.png');
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

•card-wrapper . card-front {
    position: relative; /* Ensure it is positioned relative for stacking */ z-index: 2; /* This needs to be higher than the avatar-frame */
}

.card-wrapper .survivor-card {
  transition: transform 0.6s ease;
  transform-style: preserve-3d;
  position: relative;
}

.card-wrapper.flipped .survivor-card {
  transform: rotateY(180deg);
}
